/**
 * EmptyState — Wave 5 stubs for the shared empty-state primitive (D-19).
 *
 * Co-located with the implementation at
 * `frontend/src/components/ui/empty-state.tsx` (Plan 06 creates it). Tests are
 * skipped via `describe.skip` so this file loads cleanly before the
 * implementation exists.
 */
import { describe, it } from "vitest";

describe.skip("EmptyState (Wave 5)", () => {
  it("renders large variant with size-16 illustration wrapper", () => {});
  it("renders compact variant with size-10 illustration wrapper", () => {});
  it("renders title + message", () => {});
  it("renders action slot when provided", () => {});
  it("uses provided LucideIcon when icon prop set", () => {});
  it("uses custom illustration ReactNode when illustration prop set", () => {});
  it("defaults to PackageOpenIcon circle when neither prop provided", () => {});
  it("applies className prop to root", () => {});
});
