<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.beilstein.chemxtract</groupId>
  <artifactId>cdx-render</artifactId>
  <version>0.1.0</version>
  <packaging>jar</packaging>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <batik.version>1.17</batik.version>
  </properties>

  <dependencies>
    <!-- CDX reader + datatypes; provided at runtime by the bchemxtract fat jar on the JPype classpath -->
    <dependency>
      <groupId>org.beilstein</groupId>
      <artifactId>bchemxtract</artifactId>
      <version>runtime</version>
      <scope>provided</scope>
    </dependency>
    <!-- The ported renderer classes use org.apache.commons.logging.LogFactory in
         their static initializers, so it must be SHADED IN (compile scope). The
         bchemxtract fat jar is NOT a reliable source: upstream 1.1.3 happened to
         bundle it, but 1.2.0 (built fresh in the Docker image from the latest
         upstream tag) does not. A `provided` scope therefore caused a runtime
         NoClassDefFoundError (org/apache/commons/logging/LogFactory) in the
         deployed backend. Bundling makes this jar self-contained for logging. -->
    <dependency>
      <groupId>commons-logging</groupId>
      <artifactId>commons-logging</artifactId>
      <version>1.3.4</version>
    </dependency>
    <!-- Faithful SVG generation; NOT in the fat jar -> shaded in -->
    <dependency>
      <groupId>org.apache.xmlgraphics</groupId>
      <artifactId>batik-svggen</artifactId>
      <version>${batik.version}</version>
    </dependency>
    <!-- Registers the ImageWriter SPI impls (PNG/JPEG/TIFF) that ImageHandlerBase64Encoder
         needs to inline embedded raster figures as base64 <image> hrefs; without it
         ImageWriterRegistry.getWriterFor(...) returns null and SVG generation NPEs.
         The original org.beilstein.boa module (oa/boa/pom.xml) depends on this too. -->
    <dependency>
      <groupId>org.apache.xmlgraphics</groupId>
      <artifactId>batik-codec</artifactId>
      <version>${batik.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.xmlgraphics</groupId>
      <artifactId>batik-dom</artifactId>
      <version>${batik.version}</version>
    </dependency>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.13.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>cdx-render-${project.version}</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.5.3</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals><goal>shade</goal></goals>
            <configuration>
              <createDependencyReducedPom>false</createDependencyReducedPom>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                  </excludes>
                </filter>
              </filters>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
